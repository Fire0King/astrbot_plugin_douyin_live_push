# ============================================================
# astrbot_plugin_douyin_live_push — 抖音推送插件
# ============================================================
#
# 注意：DouYin_Spider 无法通过 pip 安装（无 setup.py），
# 需单独 git clone：
#   git clone https://github.com/cv-cat/DouYin_Spider.git
#   cd DouYin_Spider && npm install
#
# ============================================================

# ─── DouYin_Spider 核心依赖 ───
requests>=2.28.0
beautifulsoup4>=4.12.0
protobuf>=4.25.0
protobuf3_to_dict>=0.1.5
PyExecJS>=1.5.1
websocket-client>=1.6.0

# ─── 工具依赖 ───
python-dotenv>=1.0.0

# ─── 图片渲染（rai=true 时需要） ───
# playwright 安装后还需执行: playwright install chromium
# playwright>=1.40.0